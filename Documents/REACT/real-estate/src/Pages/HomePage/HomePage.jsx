import React from "react";
import Hero from "../../Components/Hero/Hero";  // Ensure correct import

const HomePage = () => {
    return (
        <div>
            <Hero />
        </div>
    );
};

export default HomePage;
